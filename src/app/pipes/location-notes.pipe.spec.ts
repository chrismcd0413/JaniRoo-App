import { LocationNotesPipe } from './location-notes.pipe';

describe('LocationNotesPipe', () => {
  it('create an instance', () => {
    const pipe = new LocationNotesPipe();
    expect(pipe).toBeTruthy();
  });
});
