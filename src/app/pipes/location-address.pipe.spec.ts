import { LocationAddressPipe } from './location-address.pipe';

describe('LocationAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new LocationAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
